{
  "id": "workday_how_heard_hierarchical_chip",
  "platform": "workday",
  "fail_class": "hierarchical_no_chip",
  "description": "Workday source--source hierarchy: open category → pick leaf → chip shows leaf (not category filter) → listbox closed → verified only when readback matches picked leaf."
}
