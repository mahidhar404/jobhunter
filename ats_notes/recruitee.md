# Recruitee - known form structure

Not yet learned from a real run - `scrape_ats.py` was only just extended to
scrape Recruitee's public listing API (`scripts/scrape_ats.py:scrape_recruitee`),
which covers discovery, not the candidate-facing application form itself.

If you fill an application on a `*.recruitee.com` domain, note what you
find here afterward (field selectors, known quirks, known blockers) so the
next job on this platform benefits - same pattern as workday.md/
greenhouse.md/lever.md/ashby.md/icims.md.
