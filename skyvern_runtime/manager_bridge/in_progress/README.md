# In progress — acknowledged tasks

Cursor moves tasks here with `ack_task.py` when an agent starts work.

Do not edit task files in place except to add optional `ack_notes` in frontmatter.
When done, post to `outbox/` and archive the pair.
